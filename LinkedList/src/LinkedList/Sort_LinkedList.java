package LinkedList;

import LinkedList.Kunal_LInkedList;

public class Sort_LinkedList {

    Node merge(Node list1, Node list2){
        Node l1 = list1;
        Node l2 = list2;
        while(l1 != null || l2 != null){
            if(l1.data > l2.data)
        }
    }
}
